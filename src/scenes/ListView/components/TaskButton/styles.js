import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  button: {
    height: 50,
    width: 100,
    borderRadius: 0,
  },
  icon: {
    fontSize: 32,
  }
});

export default styles;
